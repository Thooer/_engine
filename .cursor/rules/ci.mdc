---
description: ci强制规范
alwaysApply: false
---
对于任意一个模块，有以下CI限制:
1从发现一个mod.rs开始：对mod.rs的要求是禁止包括任何形式的impl这四个字母，整个模块只要是需要pub的内容全都定义在mod.rs中。
mod.rs 禁止 include!， 其只能出现在实现文件中
mod.rs 禁止私有strcut
mod.rs 禁止存在单独的fn，所有方法必须经由trait包装，从而使得接口实现分离。
2程序会寻找同一文件夹下的所有其他非mod.rs的.rs文件
检查要求是：所有.rs必须有且只有一个实现某个trait的impl块，并且文件命名必须和这个块对应，禁止pub关键字。
3寻找下一层文件夹，如果这个文件夹叫做internal，那么这个文件夹禁止存在mod.rs，其中每个rs文件必须用{}大括号包裹，因为这些内容都是被impl文件内include的内容，不包含任何方法签名，只有函数体，目的是让impl超过400行后还能保持高信息熵化，读起来像工具书，而不是一个trait的所有细节实现，而是保留出执行流或突出逻辑的流动。
如果这个文件夹叫做tests，那么这个文件夹同样禁止存在mod.rs，必须在mod.rs中声明模块，并且必须跟在某个trait后，并且测试文件必须和trait同名。
如果这个文件夹不是internal，去发现mod.rs然后重复以上步骤

额外约束，禁止xxximpl或者xxxtests这种命名，因为impl只有一个位置，就是mod.rs同级，tests也只有一个位置，就是tests目录下。


最终形成的结构是只需要阅读mod.rs就能完整知道一个模块的所有情况的所有组合的用法。
并且，对于拆分模块的流程就变得很简单了，只需要移动impl文件，以及internal（如果有的话）和tests对应的文件