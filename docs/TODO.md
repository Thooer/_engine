# ToyEngine 开发 TODO / 里程碑路线图

> 本文是**全局开发路线图**，从"能跑起来"到"可玩 Demo / 编辑器雏形"的分阶段目标。
> 每一阶段尽量保证：**可以单独运行，有一个小 Demo，可以作为 Checkpoint 打 Tag**。

---

## 模块依赖关系总览

```
                          ┌─────────────┐
                          │  阶段 0-2   │
                          │ 基础框架    │
                          └──────┬──────┘
                                 │
                    ┌────────────┼────────────┐
                    ▼            ▼            ▼
             ┌──────────┐ ┌──────────┐ ┌──────────┐
             │ 阶段 3   │ │ 阶段 4   │ │ 阶段 5   │
             │   ECS    │ │ 资源加载 │ │ 输入/相机│
             └─────┬────┘ └─────┬────┘ └────┬─────┘
                   │            │            │
       ┌───────────┼────────────┼────────────┼───────────┐
       ▼           ▼            ▼            ▼           ▼
┌────────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
│ 阶段 6.1   │ │ 阶段 6.3 │ │ 阶段 7   │ │ 阶段 8   │ │ 阶段 9   │
│ 物理扩展   │ │ 场景图   │ │ 骨骼动画 │ │ 粒子系统 │ │   UI     │
└─────┬──────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘
      │             │            │            │            │
      └─────────────┴─────┬──────┴─────────────┴─────────────┘
                          ▼
                 ┌─────────────────┐
                 │ 阶段 10 序列化   │
                 └────────┬────────┘
                          ▼
                 ┌─────────────────┐
                 │ 阶段 11 脚本    │
                 └────────┬────────┘
                          ▼
                 ┌─────────────────┐
                 │ 阶段 12 编辑器   │
                 └─────────────────┘
```

### 关键依赖链

| 模块 | 直接依赖 | 可独立推进 |
|------|----------|------------|
| **物理扩展（射线检测）** | 渲染模块 | ✅ 需等待渲染 |
| **音频系统** | 阶段 1 窗口 | ✅ 可先做 |
| **场景图/层级** | ECS | ✅ 可先做 |
| **骨骼动画** | glTF 加载 (阶段 4) | ✅ 需等待资源加载 |
| **粒子系统** | 渲染管线 | ✅ 需等待渲染 |
| **UI 系统** | egui (阶段 5) | ⚠️ 依赖 Debug UI |
| **序列化** | 无强依赖 | ✅ 可先做 |
| **脚本系统** | 序列化 | ✅ 可先做 |
| **编辑器** | UI + 脚本 | ✅ 需前置完成 |

---

## 阶段 0：项目初始化 & 底层准备

- [ ] **仓库 & CI**
  - [ ] 初始化 Git 仓库 / Workspace 结构
  - [ ] 配置基础 CI（格式检查 + 构建）
  - [ ] 提交 `Cargo.toml` / `Cargo.lock` / `.gitignore`
- [ ] **基础依赖确认**
  - [ ] 确认技术栈中核心库版本：`winit`、`wgpu`、`bevy_ecs`、`glam` 等
  - [ ] 能在本机成功 `cargo run` 一个空程序

**阶段目标**：环境能一键拉起，CI 通过，主程序可以空跑。

---

## 阶段 1：Platform + Core（窗口 & 主循环骨架）

- [ ] **Platform 层**
  - [x] 使用 `winit` 打开窗口，正确处理关闭事件
  - [x] 整合 `raw-window-handle`，为后续 `wgpu` 做准备
- [ ] **Core / Application**
  - [x] 定义引擎生命周期：`init → update(dt) → shutdown`
  - [x] 实现主循环（固定帧 / 变步长，至少选一种）
  - [x] 集成日志系统（`tracing`），在主循环中打印基础信息

**阶段目标**：可以打开一个窗口，主循环稳定运行，日志输出可观测。

---

## 阶段 2：基础渲染通路（RHI + Renderer v0）

- [ ] **RHI / wgpu 初始化**
  - [ ] 使用 `wgpu` 创建 `Instance/Adapter/Device/Queue/Swapchain`
  - [ ] 清屏渲染：能以指定颜色 Clear 屏幕
- [ ] **绘制基础图元**
  - [ ] 使用 WGSL 着色器画出一个三角形 / 简单几何体
  - [ ] 支持窗口大小变化时正确重建 Swapchain

**阶段目标**：一个稳定的"清屏 + 三角形" Demo，证明渲染管线工作正常。

---

## 阶段 3：ECS 场景 & 渲染集成

- [ ] **ECS（`bevy_ecs`）引入**
  - [ ] 定义基础组件：Transform、Renderable 等
  - [ ] 定义基础系统：Transform 更新、渲染收集
- [ ] **Renderer v0 与 ECS 集成**
  - [ ] 从 ECS 中读取可渲染实体，统一提交到渲染层
  - [ ] 支持多个简单实体（不同位置/颜色）的渲染

**阶段目标**：在场景中放置多个实体，通过 ECS 更新，在屏幕上正确渲染。

---

## 阶段 4：资源系统 v0（Asset Loading）与基础 IO

- [ ] **File/IO & VFS**
  - [ ] 抽象统一的文件访问接口（本地文件即可）
  - [ ] 预留 VFS / 远程存储接口（可以先不实现）
- [ ] **资源加载 v0**
  - [ ] 使用 `gltf` / `image` 等库，从磁盘加载模型和纹理
  - [ ] 简单缓存（同一资源不会重复加载）
  - [ ] 支持从配置中指定要加载的场景/模型

**阶段目标**：能通过配置指定 glTF 模型 / 纹理，并在场景中渲染出来。

---

## 阶段 5：输入系统 & 相机控制 & 调试 UI v0

- [ ] **输入系统 v0**
  - [ ] 使用 `winit` 键鼠事件，建立最小的 Input 抽象
  - [ ] 支持"按键状态查询"与"按键触发事件"两种模式
- [ ] **相机控制**
  - [ ] 实现自由飞行相机（WASD + 鼠标）
  - [ ] 或实现轨道相机（围绕目标旋转）
- [ ] **Debug UI v0**
  - [ ] 集成 `egui`（`egui-winit` + `egui-wgpu`）
  - [ ] 显示 FPS / 渲染对象数量等基础信息
  - [ ] 提供几个简单开关（例如：开关 VSync / 切换渲染模式）

**阶段目标**：可以在场景中自由飞行查看模型，并通过 Debug UI 观察和切换基本状态。

---

## 阶段 6：物理 & 音频 & 更丰富的 Playground

- [ ] **物理系统 v0（rapier）** - 依赖：阶段 3-5
  - [x] 引入 `rapier3d` / `rapier2d`，创建物理世界
  - [x] 将 ECS Transform 与物理刚体同步（读/写 两个方向）
  - [x] 简单碰撞检测和地面/重力 Demo
  - [ ] **物理扩展功能** - 依赖：渲染模块就绪
    - [ ] 射线检测（Raycast）- 用于点击拾取、视线检测
    - [ ] 碰撞事件回调（碰撞开始/持续/结束）
    - [ ] 物理查询（Queries）- 区域检测、形状探测
- [ ] **音频系统 v0（kira）** - 依赖：阶段 1
  - [ ] 引入 `kira` 音频库
  - [ ] 创建 AudioContext 资源
  - [ ] 在启动时播放背景音乐
  - [ ] 触发特定事件时播放音效（例如：物体落地声）
- [ ] **场景图/层级系统** - 依赖：阶段 3 ECS
  - [ ] Parent-Child 变换层级组件
  - [ ] 层级变换传递系统（父节点移动，子节点跟随）
  - [ ] 与现有 Transform 系统集成
- [ ] **Playground 场景** - 依赖：物理、音频、相机
  - [ ] 若干物体掉落在地面，有简单交互（例如按键生成新物体）
  - [ ] 使用 Debug UI 切换不同测试场景

**阶段目标**：有一个物理+音频+相机的"小游乐场"场景，可用来验证底层各系统协作。

---

## 阶段 7：骨骼动画系统 - 依赖：阶段 4 资源加载

### 7.1 骨骼动画基础 - 依赖：glTF 加载
- [ ] 骨骼组件（Skeleton）- 存储骨骼层级和关节数据
- [ ] 动画剪辑（AnimationClip）- 播放时间线、关键帧数据
- [ ] 动画混合器（Animator）- 支持多个动画状态机
- [ ] GPU 蒙皮（Skinned Mesh）渲染支持

### 7.2 动画系统 - 依赖：7.1
- [ ] 动画状态机（简单版）- Idle/Walk/Run/Attack 等状态切换
- [ ] 动画混合 - 线性混合、叠加混合
- [ ] 事件触发 - 动画特定帧触发回调（如攻击判定）

### 7.3 与物理集成 - 依赖：阶段 6 物理
- [ ] 物理驱动动画（Ragdoll）
- [ ] 动画影响物理（脚步 IK 适配地面）

---

## 阶段 8：粒子系统 - 依赖：阶段 3 渲染

### 8.1 粒子系统基础 - 依赖：渲染管线
- [ ] 粒子组件（ParticleSystem）- 发射器配置
- [ ] 粒子更新系统 - 位置、速度、生命周期
- [ ] GPU 粒子 vs CPU 粒子选择

### 8.2 粒子渲染 - 依赖：8.1, 渲染模块
- [ ] Billboard 粒子渲染
- [ ] 纹理/颜色渐变
- [ ] 粒子光晕效果

---

## 阶段 9：UI 系统 - 依赖：阶段 5 Debug UI

### 9.1 UI 布局系统 - 依赖：egui
- [ ] 布局容器（Stack、Grid、Flex）
- [ ] UI 组件库（Button、Text、Image、Slider）
- [ ] 样式/主题系统

### 9.2 游戏中 UI - 依赖：9.1
- [ ] HUD（血条、金币、背包）
- [ ] 菜单系统
- [ ] 对话框/任务追踪

---

## 阶段 10：存档 / 序列化 & 场景配置

- [ ] **序列化基础**
  - [ ] 使用 `serde` + `bincode` / `ron` 对场景配置进行序列化
  - [ ] 至少支持：实体的 Transform、模型引用、简单属性
- [ ] **存档与加载**
  - [ ] 支持从文件加载一个场景配置并实例化
  - [ ] 支持在运行时保存当前场景到文件（调试用）

**阶段目标**：可以用数据文件描述一个场景，运行时加载/保存，实现最基础的"关卡数据驱动"。

---

## 阶段 11：脚本系统 v0（WASM）与 Gameplay 雏形 - 依赖：阶段 10 序列化

- [ ] **WASM 脚本运行时**
  - [ ] 选择 WASM 容器（如 `wasmtime` / `wasmer` 等）
  - [ ] 支持加载一个 WASM 模块并调用其中的更新函数
  - [ ] 提供基础绑定（时间、Transform 读写等）
- [ ] **Gameplay 框架 v0**
  - [ ] 定义一些通用组件（如 Health、Trigger、简单状态机等）
  - [ ] 用 WASM 编写一个简单交互逻辑（例如：靠近门自动打开）

**阶段目标**：通过脚本实现一个"小玩法 Demo"，验证"引擎驱动 + 脚本编写逻辑"的工作流。

---

## 阶段 12：编辑器雏形 & 工具化 - 依赖：阶段 9 UI + 阶段 11 脚本

- [ ] **Editor Shell v0**
  - [ ] 基于 `egui` 实现：场景层级视图 + Inspector（属性面板）
  - [ ] 支持选中实体并实时修改 Transform / 组件属性
- [ ] **基础工具链**
  - [ ] 资源浏览器：列出已加载资源，支持简单刷新/重载
  - [ ] DebugDraw：物理碰撞体、摄像机视锥等可视化

**阶段目标**：形成一个"编辑器+运行时一体"的雏形，可以在编辑器里布置物体并立即预览。

---

## 里程碑节点（建议打 Tag）

- [ ] **M0：项目可构建 & CI 通过**（阶段 0 完成）
- [ ] **M1：窗口 + 主循环 + 清屏三角形**（阶段 1–2 完成）
- [ ] **M2：ECS 场景 + 模型加载渲染**（阶段 3–4 完成）
- [ ] **M3：可飞行相机 + 调试 UI**（阶段 5 完成）
- [ ] **M4：物理 + 音频 + 场景图 + Playground**（阶段 6 完成）
- [ ] **M5：骨骼动画系统**（阶段 7 完成）
- [ ] **M6：粒子系统**（阶段 8 完成）
- [ ] **M7：UI 系统**（阶段 9 完成）
- [ ] **M8：数据驱动场景 + 存档/加载**（阶段 10 完成）
- [ ] **M9：脚本驱动的小型可玩 Demo**（阶段 11 完成）
- [ ] **M10：编辑器雏形（层级视图 + Inspector）**（阶段 12 完成）

> 后续还可以在此文件中不断细化每个阶段的子任务，或按版本号（v0.1 / v0.2 / …）拆分。
